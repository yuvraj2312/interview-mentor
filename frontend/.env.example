VITE_API_BASE_URL=http://localhost:8000

# Minutes of no user activity before an automatic logout. Suppressed
# entirely while a live interview session is in progress.
VITE_IDLE_TIMEOUT_MINUTES=1
# How many seconds before the idle timeout fires to show the
# "you'll be logged out" warning.
VITE_IDLE_WARNING_SECONDS=20
